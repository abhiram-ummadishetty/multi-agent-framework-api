apiVersion: v1
kind: Service
metadata:
  name: multi-agent-framework-api
  namespace: multi-agent
  labels:
    app: multi-agent-framework-api
spec:
  selector:
    app: multi-agent-framework-api
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
  type: ClusterIP
